# gh-stack 動作確認: 1層目（土台）

これは `gh stack` の挙動を試すための一時的なデモ用ファイルです。

このファイルはスタックの一番下（trunk である `main` に一番近い層）の PR に含まれます。
後続の層（2層目・3層目）は、この層の内容を前提として積み重なっていきます。

動作確認が終わったら、このディレクトリ（`gh-stack-demo/`）ごと削除して問題ありません。

追記: `gh stack rebase --upstack` の動作確認のために、この行を追加しました。
追記2: `gh stack push` の動作確認のために、この行を追加しました。
追記3: `gh stack push` の動作確認のために、この行を追加しました。
追記4: `gh stack sync` が trunk（main）を更新していない状態でもカスケードrebaseするかを検証するために、この行を追加しました。
追記5: 1回目の検証ではtrunk自体が別件のマージで動いてしまったため、trunkが完全に不動の状態で再検証するために、この行を追加しました。
