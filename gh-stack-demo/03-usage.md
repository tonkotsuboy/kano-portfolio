# gh-stack 動作確認: 3層目（スタックの一番上）

このファイルはスタックの一番上（trunk から一番遠い層）です。
`gh stack view --json` でこのスタック全体（1〜3層）の状態を確認できます。

## 検証後の片付け

動作確認が終わったら、以下のいずれかで片付けてください。

- 3つの PR をクローズし、ローカルブランチ（`demo/gh-stack-step-1` 〜 `3`）を削除する
- `gh stack unstack` でローカルの追跡情報だけを外す（GitHub 上の PR はそのまま残る）
